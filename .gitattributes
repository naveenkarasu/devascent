# Normalize line endings to LF in the repo (Go convention; avoids CRLF churn).
* text=auto eol=lf

# Binary-ish content that must not be line-ending-normalized.
*.png binary
*.jpg binary
*.gif binary
*.wasm binary
